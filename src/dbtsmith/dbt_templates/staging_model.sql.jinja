SELECT
    {{ column_list }}
FROM (
    SELECT
        *,
        ROW_NUMBER() OVER (
            PARTITION BY {{ dedupe_keys | join(', ') }}
            ORDER BY {{ order_by }}
        ) AS rn
    FROM {{ source_ref }}
) deduped
WHERE rn = 1